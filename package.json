{
  "name": "relese-automation-test",
  "version": "1.3.0",
  "private": true,
  "scripts": {
    "release": "release-it --ci --VV",
    "release-dry-run": "release-it --ci --VV --dry-run"
  },
  "dependencies": {
    "@release-it/bumper": "^6.0.1",
    "@release-it/conventional-changelog": "9.0.3",
    "release-it": "17.10.0"
  }
}
